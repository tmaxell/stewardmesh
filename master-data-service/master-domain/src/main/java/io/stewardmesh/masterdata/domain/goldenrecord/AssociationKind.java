package io.stewardmesh.masterdata.domain.goldenrecord;

/** Why an immutable source assertion became associated with a mastered target. */
public enum AssociationKind {
    AUTO_LINK,
    NEW_ENTITY
}
